using UnityEngine;

public class slotScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public GameObject currentItem; // item currently held by the slot 
}
